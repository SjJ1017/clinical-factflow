import sys,json
from pathlib import Path
root=Path(__file__).resolve().parents[2];sys.path.insert(0,str(root/'scripts/study_report'))
from fact_outcome import fit
p=root/'findings/medcase24-fact-outcome';D=json.loads((p/'analysis.json').read_text());O=json.loads((p/'observations.json').read_text());rows=O['traces']
loo=[]
for key in ['r2_evidence','r1_entropy','compression12']:
 vals=[];sd=next(r for r in D['tests'] if r['key']==key)['scale_sd']
 for c in sorted({r['case'] for r in rows}):
  r=fit([r for r in rows if r['case']!=c],key,controls=['log_tokens','r1_correct'],boot=30);vals.append(r['mean']/r['scale_sd']*sd)
 loo.append({'key':key,'min':min(vals),'max':max(vals),'positive':sum(v>0 for v in vals),'negative':sum(v<0 for v in vals)})
D['targeted']['leave_one_case_out_association']=loo;D['targeted']['diagnosis_tag_share_R1']=float(sum(r['r1_diagnosis_share'] for r in rows)/len(rows))
(p/'analysis.json').write_text(json.dumps(D,separators=(',',':'),allow_nan=False))
f=lambda x:f'{100*x:+.1f}'
lines=['# Early fact patterns and final diagnosis','', 'Exploratory offline audit of 120 traces on the same 24 cases. No new model calls, extraction, or labels. The fixed primary family contains 12 metrics; targeted follow-ups are explicitly separate.','', '## Main findings','', 'R2 original-evidence coverage is the strongest replication candidate. A 1-SD increase (8.9 percentage points of coverage) is associated with +11.1 pp final accepted accuracy [3.9, 18.3], after case and condition fixed effects, R1 output token volume and the fraction of R1 agents already correct. Cluster-robust p=.011; BH q=.129 across all 12 metrics. No primary metric reaches q<.05.','', 'The direction persists after removing uncertain final labels, controlling initial coverage, and excluding diagnosis-tagged facts. However, conditioning on R2 agent correctness reduces the estimate to +1.1 pp [-2.3, 5.2] per SD. This can reflect a shared reasoning state or a pathway mediated by an improved R2 diagnosis; it does not identify which causes which.','', 'Correct-source peer uptake advantage is +7.6 pp [3.9,11.2] within the same R1 panel. After removing all source units bearing a diagnosis tag, it is -0.3 pp [-4.9,3.9]. The paired change is +8.0 pp [5.2,10.7]. Diagnosis tags cover judgments and medical reasoning, not just the final-answer string. They affect 49.4% of R1 within-output units on average. This is a substantial compositional ablation, not proof that agents merely copy answers.','', 'Adding all 12 facts metrics does not improve held-out-case Brier score in the fixed ridge-logistic baseline. The audit therefore has candidate explanatory associations, not a validated failure detector.','', '## Definitions and statistical model','', '- Primary outcome: accepted R3 semantic-system diagnosis (S or L). The 120 traces contain S=55, L=29, D=22, U=14. U counts as not accepted in the main analysis, not as definitely wrong. Excluding U is a sensitivity analysis.','- Predictors use only R1, R2 and R1→R2 flow. No R3 output facts enter them. Only 13 of 24 cases vary in accepted final outcome across the five settings.','- Original-evidence coverage: fraction of equivalence units in original source cards with an identity/direct-equivalence match in the union of the three agents’ outputs for that round. Metadata is excluded. This is case-evidence coverage, not coverage of clinically necessary clues.','- Novel share: fraction of R2 output units without an identity/direct-equivalence match in any R1 output.','- Domain entropy: fractional domain mass over eight categories, divided by log2(8). Professional uptake prime uses the existing fractional scheme.','- Each primary feature enters a separate linear probability model standardized by its overall sample SD. Controls: case and condition fixed effects, log R1 visible-output tokens, R1 accepted agent fraction. The latter uses gold labels, so this is an explanatory adjustment, not a deployable predictor.','- CIs: 2,000 case-bootstrap resamples. p: case-cluster sandwich covariance with finite-sample correction and t distribution, G−1 degrees of freedom. q: BH across the fixed 12 primary coefficients. Bootstrap CI and cluster-t p can disagree near zero because they are different small-sample approximations.','- Controls are not causal identification: R1 correctness may mediate R1 fact effects, while R2 correctness can mediate or confound later associations. Conditioning on final success to explain uptake can also create selection effects.','', '## Full primary family','', '| Metric | Accuracy difference per 1 SD, pp | 95% case-bootstrap CI | Cluster p | BH q |','|---|---:|---:|---:|---:|']
for r in D['tests']:lines.append(f"| {r['name']} | {f(r['mean'])} | [{f(r['lo'])}, {f(r['hi'])}] | {r['p']:.3f} | {r['q']:.3f} |")
lines+=['','R1 entropy has a negative tendency (-8.5 pp [-15.6,+0.2], q=.292), but is sensitive to grading/adjustment and may reflect diagnosis-label concentration. Compression and novelty are less robust: their associations weaken markedly when uncertain diagnoses are excluded. Increasing n will not automatically remove these biases or guarantee significance.','', '## Coverage robustness, common effect scale','', 'All rows below report accuracy difference for +10 percentage points of coverage.','', '| Analysis | Difference, pp | 95% CI, pp |','|---|---:|---:|']
z=[('Primary',next(r for r in D['tests'] if r['key']=='r2_evidence'))]+[(r['sensitivity'],r) for r in D['sensitivity'] if r['key']=='r2_evidence' and r['sensitivity'] in ['exclude_U','same_only','adjust_R2_correctness']]+[('Control R1 coverage',D['targeted']['extra'][0]),('Exclude diagnosis tags',D['targeted']['extra'][2])]
for name,r in z:
 k=.1/r['scale_sd'];lines.append(f"| {name} | {f(r['mean']*k)} | [{f(r['lo']*k)}, {f(r['hi']*k)}] |")
lines+=['','Targeted checks above were added after reading the initial results. They are sensitivity analyses, not independent confirmations. The R2 coverage coefficient stays positive when each of the 24 cases is omitted in turn.','', '## Why a source might receive more uptake','', 'Only R1→R2 peer edges are used, without stratifying by final outcome. Both source and recipient R1 diagnoses must have S/L/D labels (472 edges, 24 cases). The within-panel model uses case×condition fixed effects. The adjusted model additionally controls recipient correctness, prior same diagnosis, pre-existing fact overlap and log source fact count. Source units have equal contribution to an edge rate; edges enter the model equally, inference clusters by case.','', '| Measure | Adjustment | Correct-source advantage, pp | 95% CI |','|---|---|---:|---:|']
for r in D['upstream']:lines.append(f"| {r['measure']} | {r['adjustment']} | {f(r['mean'])} | [{f(r['lo'])}, {f(r['hi'])}] |")
lines+=['','After adjustment, +10 pp of source facts already present in the recipient’s R1 output predicts +2.8 pp uptake [1.0,4.4]. The adjusted source-correctness contrast is +5.4 pp [-2.1,12.0], and same-prior-diagnosis contrast is +2.9 pp [-4.1,9.9]. Familiarity is an observable antecedent, but the result is not randomized. Prior overlap and later uptake share source-fact denominators, and semantic matching errors may affect both.','', '## Held-out-case prediction','', 'Leave one entire case out, holding all five settings together. Standardization and median imputation are fitted inside each training fold. Fixed logistic L2 C=1, no hyperparameter search. Observable baseline: setting, R1 log tokens, R1 pairwise answer agreement. Oracle baseline adds R1 accepted agent fraction. Fact models add all 12 prespecified metrics.','', '| Model | Brier (lower better) | AUROC | Log loss |','|---|---:|---:|---:|']
for name,r in D['cv']['models'].items():lines.append(f"| {name} | {r['brier']:.3f} | {r['auc']:.3f} | {r['log_loss']:.3f} |")
lines+=['','Brier improvement (baseline minus fact model):']
for r in D['cv']['comparisons']:lines.append(f"- {r['baseline']}: {r['brier_improvement']:+.3f} [{r['lo']:+.3f},{r['hi']:+.3f}].")
lines+=['','These intervals bootstrap the 24 held-out case losses; training-set uncertainty is not included. A negative Brier improvement means worse prediction. No claim that every possible fact-based model will fail; this fixed model lacks demonstrated incremental generalization.','', '## Causal next step','', 'Freeze R1 outputs, then randomize which original-evidence facts appear in R2 peer messages while matching message length, source identity and diagnosis wording. Separately vary whether a fact is already familiar to the recipient. Measure R2 evidence recovery and blinded R3 diagnosis. This intervenes on evidence exposure before the outcome and tests whether recovered evidence causes a correction. Diagnosis wording can be held fixed or randomized as its own factor to distinguish evidence uptake from conclusion alignment. No such intervention has been run here.','', 'The pilot still lacks clinician-reviewed fact truth/relevance, independent repeated seeds, and enough independent cases for stable high-dimensional prediction. Case selection, one trace per condition, uncertainty in diagnosis grading, and imperfect matching remain material limitations. Neither temporal order nor a larger observational sample alone identifies the causal path.','', 'Reproduce: `.venv/bin/python scripts/study_report/fact_outcome.py`, then `scripts/study_report/fact_outcome_followup.py`, then `scripts/study_report/report_fact_outcome.py`. Source observations, all regression points and prediction probabilities are retained under `findings/medcase24-fact-outcome/`.']
text='\n'.join(lines)+'\n';(p/'README.md').write_text(text);(root/'docs/medcase24-fact-outcome.md').write_text(text)
print('Leave-one-case-out signs',loo)
